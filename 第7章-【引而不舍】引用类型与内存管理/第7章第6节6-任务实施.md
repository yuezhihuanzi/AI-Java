# 第7章 第6节 任务实施

> 原文：[逻辑帧课程页](https://www.logamee.com/course-learning/77/1513)  
> 整理原则：按网页正文走；只改笔误和明显不通的句子；重点用加粗和颜色标出。

---

> **阅读指南**  
> 本节完成本章任务：用字符串拼接格式化档案，用 `StringBuilder` 生成战斗日志，并理解引用类型比较。

---

## 6.1 任务要求

1. 把 `Player` 信息拼成完整文字，包含名字、等级、HP、MP、攻击、防御、金币。
2. 用 `StringBuilder` 逐行拼接战斗过程，返回完整日志字符串。
3. 用 `String.join` 或循环把技能列表、背包物品列表拼成一行文字。
4. 创建两个名字相同的 `Enemy` 对象，用 `==` 和 `equals()` 比较并解释结果。

---

## 6.2 [P] 预测——两个 "hello" 相等吗

```java
class StringDemo {
    public static void main(String[] args) {
        String s1 = "hello";
        String s2 = "hello";
        String s3 = new String("hello");

        System.out.println("s1 == s2: " + (s1 == s2));
        System.out.println("s1 == s3: " + (s1 == s3));
        System.out.println("s1.equals(s2): " + s1.equals(s2));
        System.out.println("s1.equals(s3): " + s1.equals(s3));
    }
}
```

预测后运行代码，验证你的预测是否正确。想一想：`==` 和 `equals()` 分别比较的是什么？到 `profile/answers` 对照输出结果。

---

## 6.3 [I] 研究——比较 Enemy 对象

```java
class Enemy {
    String name;
    int hp;

    Enemy(String name, int hp) {
        this.name = name;
        this.hp = hp;
    }
}

public class EnemyCompare {
    public static void main(String[] args) {
        Enemy e1 = new Enemy("影子怪", 20);
        Enemy e2 = new Enemy("影子怪", 30);

        System.out.println("e1 == e2: " + (e1 == e2));
        System.out.println("e1.name.equals(e2.name): "
                + e1.name.equals(e2.name));
        System.out.println("e1.hp == e2.hp: " + (e1.hp == e2.hp));
    }
}
```

思考：

- `e1 == e2` 为什么是 `false`？
- `e1.name.equals(e2.name)` 为什么是 `true`？
- `e1.hp == e2.hp` 比较的是什么？

---

## 6.4 [M] 完成——信息显示模块

独立实现一个信息显示类。动手前先想清楚：

- `formatProfile` 用字符串拼接展示玩家档案，哪些信息要展示？换行怎么处理？
- `generateBattleLog` 用 `StringBuilder` 逐行拼接战斗过程，参数有哪些？返回什么？
- 技能列表、背包物品列表如何拼成一行文字？`String.join` 的用法是什么？
- 创建两个名字相同的 `Enemy`，`==` 和 `equals()` 比较结果一样吗？为什么？

> **动笔时间**  
> <font color="orange">暂停阅读，先独立实现，再对照参考答案（`profile/answers`）。</font>

---

## 相对网页原文改了什么

只动明确笔误 / 病句，不动教学内容。

| 原文 | 现写法 |
|------|--------|
| `[!TIP]` 阅读指南 / 动笔时间 | 普通引用块 |
